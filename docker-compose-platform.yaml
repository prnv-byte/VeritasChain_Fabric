services:
  mongodb:
    image: mongo:7
    container_name: veritaschain-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - veritaschain

volumes:
  mongodb_data:

networks:
  veritaschain:
    name: veritaschain
    external: true
