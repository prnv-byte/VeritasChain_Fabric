# ── Generated crypto material — NEVER commit (contains private keys) ──────────
network/organizations/peerOrganizations/
network/organizations/ordererOrganizations/
network/organizations/fabric-ca/*/msp/
network/organizations/fabric-ca/*/tls-cert.pem
network/organizations/fabric-ca/*/IssuerPublicKey
network/organizations/fabric-ca/*/IssuerRevocationPublicKey
network/organizations/fabric-ca/*/ca-cert.pem
network/organizations/fabric-ca/*/fabric-ca-server.db

# ── Generated at runtime — rebuild with scripts ────────────────────────────────
ledger/
prototype/ledger/
network/ledger/
network/channel-artifacts/

# ── Node dependencies — install with npm install ───────────────────────────────
node_modules/
backend/node_modules/
frontend/node_modules/

# ── Lock files ─────────────────────────────────────────────────────────────────
backend/package-lock.json
frontend/package-lock.json

# ── Go vendor — regenerate with: go mod vendor ────────────────────────────────
chaincode/order/vendor/

# ── Claude Code workspace ──────────────────────────────────────────────────────
.claude/

# ── Go build artifacts ─────────────────────────────────────────────────────────
chaincode/*/*.test
chaincode/order/order

# ── Environment files ─────────────────────────────────────────────────────────
.env
*.env
backend/.env

# ── OS files ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Misc ───────────────────────────────────────────────────────────────────────
deploy-output.txt
prototype/deploy-output.txt
